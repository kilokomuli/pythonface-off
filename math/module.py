from add_0 import add

if __name__ == '__main__':
    """
    This script demonstrates the usage of the add function from the add_0 module.
    It adds two numbers and prints the result.
    """

    a = 1
    b = 2
    print(f"{a:d} + {b:d} = {add(a, b)}")